package main

// REDIS
// struct ? maybe just methods ? some repository for depth events ?
// need a service or repo struct for redis, and methods to handle updating the order book
// need to think of a structure of order book in Redis
// also need to think of how to handle the updates in the order book.
